# ~/.tmuxinator/collective.yml

name: collective
root: ~/code/collective-archive-site-generator
pre_window: export HISTFILE=~/.bash_history-collective

windows:
  - editor:
      layout: main-horizontal
      panes:
        - vim
        - grunt watch
